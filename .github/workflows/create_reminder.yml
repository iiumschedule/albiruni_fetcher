name: "create reminder"

on:
  issue_comment:
    types: [created, edited]

permissions:
  issues: write
  pull-requests: write

jobs:
  reminder:
    runs-on: ubuntu-slim

    steps:
      - name: check for reminder
        uses: agrc/create-reminder-action@v1
